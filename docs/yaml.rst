Yaml
====

Yaml is a data format used mainly for configuration.
The structure of the yaml files is the same as that of the json files,
but more human-friendly. I choosed it because json is a lot less convenient
for writing.
